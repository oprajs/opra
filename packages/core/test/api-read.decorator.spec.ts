// import {RESOURCE_ENTITY_ACTIONS} from '../src/constants';
// import {ApiRead} from '../src/decorators/api-read.decorator';

describe('ApiRead() decorator', function () {
/*
  it('Should define read action metadata', async () => {
    const AnimalResource = class {
      read() {
        //
      }
    }
    ApiRead({description: 'Read animal resource'})(AnimalResource.prototype, 'read');
    const actions = Reflect.getMetadata(RESOURCE_ENTITY_ACTIONS, AnimalResource.prototype);
    expect(actions).toBeDefined();
    expect(actions.read).toBeDefined();
  })
*/
});

