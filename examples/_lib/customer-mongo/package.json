{
  "name": "customer-mongo",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "module": "./index.js",
  "dependencies": {
    "@opra/common": "^0.33.13",
    "@opra/mongodb": "^0.33.13"
  },
  "scripts": {
    "_build": "tsc && npm run check && npm run lint && npm run format",
    "lint": "eslint . --max-warnings=0",
    "check": "madge --circular src/**",
    "format": "prettier . --write --log-level=warn"
  }
}
