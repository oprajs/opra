{
  "name": "example-customer-mongo",
  "private": true,
  "version": "1.29.4",
  "type": "module",
  "module": "./index.js",
  "dependencies": {
    "@opra/common": "^1.29.4",
    "@opra/mongodb": "^1.29.4",
    "mongodb": "^7.5.0",
    "ts-gems": "^3.12.0"
  },
  "scripts": {
    "_build": "tsc && npm run check && npm run lint && npm run format",
    "lint": "eslint . --max-warnings=0",
    "check": "dpdm -T --no-warning --no-tree ./index.ts",
    "format": "prettier . --write --log-level=warn"
  }
}